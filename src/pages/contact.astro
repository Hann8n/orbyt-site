---
export const prerender = false;

import DocumentColorScheme from '../components/DocumentColorScheme.astro';
import SiteFooter from '../components/SiteFooter.astro';
import SkipToContent from '../components/SkipToContent.astro';
import { SITE_DOCUMENT } from '../utils/site-document-theme';
import { ClientRouter } from 'astro:transitions';
import { useTranslations, localeToHtmlLang } from '../i18n/utils';

const locale = Astro.locals.locale ?? 'en';
const countryCode = Astro.locals.countryCode;
const t = useTranslations(locale);
---
<!DOCTYPE html>
<html lang={localeToHtmlLang(locale)} class="orbyt-doc-dark">
<head>
    <meta charset="UTF-8">
    <DocumentColorScheme
      colorScheme="dark"
      themeColor={SITE_DOCUMENT.orbytBlack}
      tileColor={SITE_DOCUMENT.orbytBlack}
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{t('contact.title')}</title>
    <ClientRouter />

    <link rel="preload" href="/fonts/Figtree/Figtree-VariableFont_wght.woff2" as="font" type="font/woff2" crossorigin>

    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/x-icon" href="/favicon/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link rel="stylesheet" href="/css/tokens.css" />
    <link rel="stylesheet" href="/css/site-footer.css" />
    <link rel="stylesheet" href="/css/site-document.css" />
</head>
<body>
    <SkipToContent />

    <div class="site-doc">
        <div class="site-doc__content">
            <a href="/" class="site-doc__logo">
                <img src="/images/orbyt-logo.svg" alt={t('contact.logo.alt')}>
            </a>
            <div class="site-doc__content-main">
                <header class="site-doc__masthead">
                <h1>{t('contact.heading')}</h1>
                </header>

                <main id="main-content" class="site-doc__prose">

                    <h2>{t('contact.section.general')}</h2>
                    <p><a href="mailto:hello@getorbyt.com">hello@getorbyt.com</a></p>

                    <h2>{t('contact.section.press')}</h2>
                    <p><a href="mailto:press@getorbyt.com">press@getorbyt.com</a></p>

                    <h2>{t('contact.section.bugs')}</h2>
                    <p><a href="mailto:support@getorbyt.com">support@getorbyt.com</a></p>

                    <h2>{t('contact.section.copyright')}</h2>
                    <p set:html={t('contact.copyright.html')} />
                </main>
            </div>
        </div>

        <SiteFooter locale={locale} currentPath="/contact" countryCode={countryCode} />
    </div>

</body>
</html>
