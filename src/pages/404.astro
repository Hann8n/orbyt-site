---
export const prerender = true;

import DocumentColorScheme from '../components/DocumentColorScheme.astro';
import SiteFooter from '../components/SiteFooter.astro';
import { SITE_DOCUMENT } from '../utils/site-document-theme';
---

<!doctype html>
<html lang="en" class="orbyt-doc-dark">
<head>
    <meta charset="UTF-8">
    <DocumentColorScheme
      colorScheme="dark"
      themeColor={SITE_DOCUMENT.orbytBlack}
      tileColor={SITE_DOCUMENT.orbytBlack}
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found | Orbyt</title>
    
    <!-- Critical: Load fonts immediately, before any external resources -->
    <link rel="stylesheet" href="/css/fonts.css">
    <!-- Preload Figtree font to prevent FOUT -->
    <link rel="preload" href="/fonts/Figtree/Figtree-VariableFont_wght.woff2" as="font" type="font/woff2" crossorigin>
    
    <!-- Favicon setup -->
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/x-icon" href="/favicon/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link rel="stylesheet" href="/css/tokens.css" />
    <link rel="stylesheet" href="/css/site-footer.css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Figtree', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--orbyt-page-bg);
            min-height: 100vh;
            min-height: 100dvh;
            color: var(--orbyt-white);
            overflow-x: hidden;
        }

        .container {
            width: 100%;
            max-width: var(--site-content-max-width);
            margin-inline: auto;
            padding: var(--site-page-padding-y) var(--site-page-padding-x);
            min-height: 100vh;
            min-height: 100dvh;
            display: flex;
            flex-direction: column;
        }

        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            gap: clamp(0.75rem, 2vw, 1rem);
            padding-block: clamp(2rem, 8vh, 6rem);
        }

        .error-actions {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.625rem;
            flex-wrap: wrap;
            margin-top: 0.75rem;
        }

        /* Hero element - TV static is the focal point */
        .tv-static {
            width: clamp(120px, 20vw, 160px);
            aspect-ratio: 1;
            background-image: url('/images/tv-static-sprite-sheet.webp');
            background-size: 500% 500%;
            animation: tv-static-animation 0.8s steps(1) infinite;
        }

        @keyframes tv-static-animation {
            0%    { background-position: 0% 0%; }
            4%    { background-position: 25% 0%; }
            8%    { background-position: 50% 0%; }
            12%   { background-position: 75% 0%; }
            16%   { background-position: 100% 0%; }
            20%   { background-position: 0% 25%; }
            24%   { background-position: 25% 25%; }
            28%   { background-position: 50% 25%; }
            32%   { background-position: 75% 25%; }
            36%   { background-position: 100% 25%; }
            40%   { background-position: 0% 50%; }
            44%   { background-position: 25% 50%; }
            48%   { background-position: 50% 50%; }
            52%   { background-position: 75% 50%; }
            56%   { background-position: 100% 50%; }
            60%   { background-position: 0% 75%; }
            64%   { background-position: 25% 75%; }
            68%   { background-position: 50% 75%; }
            72%   { background-position: 75% 75%; }
            76%   { background-position: 100% 75%; }
            80%   { background-position: 0% 100%; }
            84%   { background-position: 25% 100%; }
            88%   { background-position: 50% 100%; }
            92%   { background-position: 75% 100%; }
            96%   { background-position: 100% 100%; }
        }

        /* Primary text - the error code */
        .error-title {
            font-size: clamp(2rem, 5vw, 2.5rem);
            font-weight: 900;
            line-height: 1;
            letter-spacing: -0.02em;
        }

        /* Supporting text - explains the error */
        .error-message {
            font-size: clamp(1rem, 2vw, 1.125rem);
            color: var(--orbyt-text-subtle);
            max-width: 32ch;
            line-height: 1.5;
        }

        /* Action - clear call to action */
        .nav-button {
            display: inline-flex;
            background: #f3f5fe;
            color: #000000;
            text-decoration: none;
            padding: 0.625rem 1.25rem;
            border-radius: 999px;
            font-size: 0.875rem;
            font-weight: 700;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .nav-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(255, 255, 255, 0.1);
        }

        .nav-button:active {
            transform: translateY(0);
        }

        .nav-button--secondary {
            background: rgba(243, 245, 254, 0.06);
            color: var(--orbyt-neutral-200);
            border: 1px solid rgba(224, 235, 255, 0.35);
            font-weight: 600;
        }

        .nav-button--secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(255, 255, 255, 0.06);
            border-color: rgba(224, 235, 255, 0.5);
        }

    </style>
</head>
<body>
    <div class="container">
        <main class="main-content">
            <div class="tv-static" aria-hidden="true"></div>
            <h1 class="error-title">No Signal</h1>
            <p class="error-message">This page doesn't exist or has been moved.</p>
            <div class="error-actions">
                <a href="#" onclick="window.history.back(); return false;" class="nav-button">Go Back</a>
                <a href="/" class="nav-button nav-button--secondary">Homepage</a>
            </div>
        </main>

        <SiteFooter />
    </div>
</body>
</html>