---
import { SUPPORTED_LOCALES, DEFAULT_LOCALE, LOCALE_LABELS, LOCALE_FLAGS, LOCALE_CODES } from '../i18n/utils'

interface Props {
  currentLocale: string
  currentPath?: string
}

const { currentLocale, currentPath = '/' } = Astro.props
const allLocales = [DEFAULT_LOCALE, ...SUPPORTED_LOCALES]

---

<div class="lang-switcher" role="group" aria-label="Language">
  <span class="lang-flag" aria-hidden="true">{LOCALE_FLAGS[currentLocale]}</span>
  <span class="lang-code">{LOCALE_CODES[currentLocale]}</span>
  <select
    id="lang-select"
    class="lang-select"
    onchange="orbytSwitchLang(this.value)"
  >
    {allLocales.map((locale) => (
      <option value={locale} selected={locale === currentLocale}>
        {LOCALE_LABELS[locale]}
      </option>
    ))}
  </select>
</div>

<script is:inline define:vars={{ SUPPORTED_LOCALES, DEFAULT_LOCALE, currentPath }}>
  window.orbytSwitchLang = function (locale) {
    document.cookie = `orbyt-locale=${locale};max-age=31536000;path=/;samesite=lax`

    let path = currentPath
    for (const l of SUPPORTED_LOCALES) {
      if (path === `/${l}` || path.startsWith(`/${l}/`)) {
        path = path.slice(l.length + 1) || '/'
        break
      }
    }

    const dest = locale === DEFAULT_LOCALE
      ? (path || '/')
      : `/${locale}${path === '/' ? '/' : path}`

    window.location.href = dest
  }
</script>

<style>
  .lang-switcher {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    cursor: pointer;
    line-height: 1;
  }

  .lang-flag {
    display: block;
    font-size: 18px;
    line-height: 1;
  }

  .lang-code {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--orbyt-neutral-500);
  }

  .lang-select {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    cursor: pointer;
    font-size: 16px;
    appearance: none;
    -webkit-appearance: none;
    border: none;
    background: none;
    padding: 0;
    margin: 0;
  }

  .lang-select option {
    background: #111;
    color: #f3f5fe;
  }
</style>
